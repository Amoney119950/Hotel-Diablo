/*
// STEP EVENT

if(state == "left")
{
	hspeed = -2;
}
else if(state == "right")
{
	hspeed = 2;
}


if(!place_free(x + hspeed, y))
{
	if(state == "left")
	{
		state = "right"
	}
	else
	{
		state = "left"
	}
}

if(place_free(x + hspeed, y) && place_free(x + hspeed *16, y + 16))
{
	if(state == "left")
	{
		state = "right"
	}
	else
	{
		state = "left"
	}
}
*/
