x += lengthdir_x(spd,direction)
y += lengthdir_y(spd,direction)

if (place_meeting(x,y,pShootable))
{
	with (instance_place(x,y,pShootable))
	{
		hp--;
		flash = 3
		hitfrom = other.directio}
		
		instance_destroy()
}


if (place_meeting(x,y,obj_floor)) && (image_index != 0)
{
	while (place_meeting(x,y,obj_floor))
	{
		x -= lengthdir_x(1,direction)
		y -= lengthdir_y(1,direction)
	}
	spd = 0
	instance_change(obj_HitSPark,true)
}

if (place_meeting(x,y,obj_snake_parent)) && (image_index != 0)
{
	while (place_meeting(x,y,obj_snake_parent))
	{
		x -= lengthdir_x(1,direction)
		y -= lengthdir_y(1,direction)
	}
	spd = 0
	instance_change(obj_HitSPark,true)
}

if (place_meeting(x,y,obj_spider_parent)) && (image_index != 0)
{
	while (place_meeting(x,y,obj_spider_parent))
	{
		x -= lengthdir_x(1,direction)
		y -= lengthdir_y(1,direction)
	}
	spd = 0
	instance_change(obj_HitSPark,true)
}